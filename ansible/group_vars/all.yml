---
# Global variables for all host groups.
# Secrets are passed at runtime via --extra-vars from the DataForge backend.
# NEVER store passwords in this file.
#
# For Ansible Vault usage in production:
#   ansible-vault encrypt group_vars/all.yml
